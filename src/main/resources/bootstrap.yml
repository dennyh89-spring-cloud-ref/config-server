server:
  port: 8888

management:
  context-path: /admin
    
#eureka:
 # instance:
  #  leaseRenewalIntervalInSeconds: 10
   # statusPageUrlPath: /admin/info
    #healthCheckUrlPath: /admin/health

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dennyh89/spring-cloud-ref-config-repo.git
          basedir: config-repo
          clone-on-start: true
          timeout: 30