spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dennyh89-spring-cloud-ref/config-repo
          basedir: config-repo
          clone-on-start: true
          timeout: 30
server:
  port: 8888